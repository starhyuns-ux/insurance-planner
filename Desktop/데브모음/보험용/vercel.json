{
  "buildCommand": "npm run build:web",
  "outputDirectory": "dist_web",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- src index.html package.json vite.config.ts tsconfig.json vercel.json"
}
